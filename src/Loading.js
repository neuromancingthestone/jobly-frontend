import React from "react";
import "./css/Loading.css"

// Loading Message Component
// Shows when a page is loading data from DB

function LoadingMessage() {
  return (
    <div className="Loading">
      Loading Page...
    </div>
  );
}

export default LoadingMessage;